<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Fotorol</title>
  </head>
  <body>
    <h1>Contact Us</h1>

    <!--
      The `action` attribute on forms is similar to the `href` attribute.
      It specifies where the data of the form will be sent.

      If left empty, the form will send the data to the current url.
      If given a url, the form will send the data to that location.
    -->
    <form action="/contact_us" method="POST">
      <div>
        <label for="fullName">Full Name</label> <br/>
        <!-- The `name` attribute is required in all input fields.
             On the server, the value will have the `name` as its
             associated key.
        -->
        <input type="text" name="fullName" id="fullName" value="">
      </div>

      <div>
        <label for="message">Message</label> <br/>
        <textarea name="message" id="message" rows="8" cols="80"></textarea>
      </div>

      <div>
        <input type="submit" value="Submit">
      </div>
    </form>

  </body>
</html>
